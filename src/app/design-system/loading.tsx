import DesignSystemShowcaseSkeleton from "./DesignSystemShowcaseSkeleton";

// Shown by Next.js during the RSC fetch when navigating to /design-system.
export default function DesignSystemLoading() {
  return <DesignSystemShowcaseSkeleton />;
}
