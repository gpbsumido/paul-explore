import AmbientBackground from "@/components/AmbientBackground";

/**
 * Ambient backdrop for the graph. A thin wrapper around the shared
 * {@link AmbientBackground} with the landing's default violet/blue aurora.
 */
export default function GraphBackground() {
  return <AmbientBackground />;
}
